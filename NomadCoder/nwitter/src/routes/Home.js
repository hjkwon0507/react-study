import React from "react";

const Home = () => <div>
  <form>
    <input type="text" placeholder="What's on yout mind?" maxLength={120} />
    <input type="submit" value="Nweet" />
  </form>
</div>

export default Home;